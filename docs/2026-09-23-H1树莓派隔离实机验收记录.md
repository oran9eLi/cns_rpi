# 2026-09-23 H1 树莓派与 F407 隔离实机验收记录

## 验收范围与环境

本次只验证树莓派主进程经现有 CH340 串口与当前烧录的 F407 之间的 H1 回显，不验证 Server/Web 全链路。用户确认 F407 已烧录支持 H1 的固件；其精确提交及 UART6 逐线接线尚未独立核验。

- 实体 Pi：ARM64，候选提交 `f0eefb2` 在 `/tmp/cns-rpi-h1-20260923` 构建，Debug 构建成功，45/45 项 CTest 通过；候选二进制为 `/tmp/cns-rpi-h1-build/cns_rpi`。
- 原生产进程：提交 `a618548`，测试前后均为 `cns-rpi.service`；测试期间短暂停止，使候选进程成为 `/dev/ttyUSB0` 的唯一读写者。CH340 USB ID 为 `1a86:7523`，识别的 F407 端点为 `sysid=76, compid=193`，设备 ID 为 `DCDWCNS1GHC0G6LF8MY6`。
- MQTT：临时 Mosquitto 仅监听 `127.0.0.1:18889` 与 `::1:18889`；测试配置使用命名空间 `cns_rpi_h1_local`、无 MQTT 凭据、临时身份绑定副本，未修改 `/var/lib/cns-rpi/config.json`，也未连接现网 Broker。临时 Broker 测试后已关闭。
- 证据原件暂存于实体 Pi 的 `/tmp/cns-h1-field-check/`：`request-*.json`、`ack-*.json`、`app.log` 与 `broker.log`。该路径位于临时目录，不能作为长期归档。

准备阶段曾用无交互 `sudo` 尝试停止原服务，但现场需要密码，停止失败；候选进程短暂启动后只报告 `/dev/ttyUSB0` 设备忙，未打开第二个串口读者，也未发送 H1 帧，随即终止。随后通过交互授权先确认原服务停止，才开始以下唯一读写者的正式测试。

## 真实请求与结果

以下时间戳为 UTC；本地时间为北京时间 UTC+8。测试请求现已过期，仅作为证据，不可直接重放。

第一条请求（2026-09-23 19:12:23 北京时间）：

```json
{"schema_version":1,"command_id":"c6b66ecc-e857-4c5a-b54b-6e2a610bce0c","request_id":"a5a1db74-df03-463b-b188-4fd1a532e98d","target":{"device_id":"DCDWCNS1GHC0G6LF8MY6"},"session_id":"0b9c6a8e-8fec-4a91-a420-b90e72dfcd7e","action_id":"a5a1db74-df03-463b-b188-4fd1a532e98d","lease_version":1,"operation":"uart_echo","text":"H1 LIVE 01","expires_at":"2026-09-23T11:13:22.731Z"}
```

实体 F407 原样回显后由 Pi 发布的完整 ACK：

```json
{"action_id":"a5a1db74-df03-463b-b188-4fd1a532e98d","command_id":"c6b66ecc-e857-4c5a-b54b-6e2a610bce0c","fact":{"f407_baud_rate":115200,"fresh_business_frame":true,"nonce":"5a2757cae53abfb5","pi_baud_rate":115200,"request_text":"H1 LIVE 01","response_text":"H1 LIVE 01","round_trip_us":12228,"rx_frame_bytes":41,"rx_frame_hex":"FD1D0000214CC181010003804CBF190102B5BF3AE5CA57275A0A4831204C49564520303100C2018721","tx_frame_bytes":38,"tx_frame_hex":"FD1A0000004CBF81010003804CC1150101B5BF3AE5CA57275A0A4831204C495645203031C604"},"observed_at":"2026-09-23T11:12:24.112Z","operation":"uart_echo","schema_version":1,"session_id":"0b9c6a8e-8fec-4a91-a420-b90e72dfcd7e","status":"completed"}
```

第二条请求（中英混合）：

```json
{"schema_version":1,"command_id":"fffd3b39-26f1-4c23-99f8-c1cecd82ac38","request_id":"3c822d16-05ff-4b23-ac31-2c6ec8cc8f1e","target":{"device_id":"DCDWCNS1GHC0G6LF8MY6"},"session_id":"e454aa96-a74d-4e3d-9203-3a3556b311d8","action_id":"3c822d16-05ff-4b23-ac31-2c6ec8cc8f1e","lease_version":1,"operation":"uart_echo","text":"你好 CNS","expires_at":"2026-09-23T11:13:44.133Z"}
```

对应完整 ACK：

```json
{"action_id":"3c822d16-05ff-4b23-ac31-2c6ec8cc8f1e","command_id":"fffd3b39-26f1-4c23-99f8-c1cecd82ac38","fact":{"f407_baud_rate":115200,"fresh_business_frame":true,"nonce":"22a56cc6380aa7b3","pi_baud_rate":115200,"request_text":"你好 CNS","response_text":"你好 CNS","round_trip_us":14157,"rx_frame_bytes":41,"rx_frame_hex":"FD1D0000A04CC181010003804CBF190102B3A70A38C66CA5220AE4BDA0E5A5BD20434E5300C201F23C","tx_frame_bytes":38,"tx_frame_hex":"FD1A0000014CBF81010003804CC1150101B3A70A38C66CA5220AE4BDA0E5A5BD20434E533307"},"observed_at":"2026-09-23T11:12:45.171Z","operation":"uart_echo","schema_version":1,"session_id":"e454aa96-a74d-4e3d-9203-3a3556b311d8","status":"completed"}
```

两条 ACK 的 `request_text=response_text`、`pi_baud_rate=f407_baud_rate=115200`、`fresh_business_frame=true`；独立以十六进制解码复算的 TX/RX 长度分别为 38/41 字节，与 ACK 一致。这里的速率是 Pi 已打开 fd 的应用值与实体 F407 回显自报值，不是从示例配置推断。

过期请求 `action_id=69867f18-dacd-449d-8c8d-4c05a7ae4271` 返回：

```json
{"action_id":"69867f18-dacd-449d-8c8d-4c05a7ae4271","command_id":"e5cf5554-15be-41f4-8b81-47ead3218b75","error_code":"action_expired","observed_at":"2026-09-23T11:13:05.619Z","operation":"uart_echo","schema_version":1,"session_id":"57e926e8-f03e-4a01-8f7e-56e6586698bc","status":"rejected"}
```

随后重复发布第一条原请求，收到相同 `action_id`、`status`、`fact`，而 H1 串口发帧日志计数在重复前后均为 2；该重复没有再次物理执行。过期拒绝也未增加发帧计数。

## 现场恢复与剩余边界

隔离候选进程结束后，原 `cns-rpi.service` 自动恢复为 `active`，PID 5670 独占 `/dev/ttyUSB0`；日志重新报告 MQTT 已连接、设备身份已核验、业务在线。临时 Broker 已停止，端口 18889 不再监听。此次没有替换生产二进制或配置，没有修改 F407。

现网 Broker 当前允许匿名连接且未发现 Topic ACL，直接启用 H1 可能绕过 Server 来源门禁；生产路由与后端构建产物尚未检出 H1。F407 精确实烧版本、UART6 逐线接线、纯中文及错误 nonce/来源/文本/波特率等负向现场向量、现网 Session 门禁、Server 入库和 Web 展示仍未验收。因此结论仅为**当前 Pi↔F407 H1 原样回显的所测路径通过**，不代表阶段一 H1 跨端验收完成或可部署到现网。
