# 阶段一 H1 真实回显：树莓派端实现、隔离实测与待验收

日期：2026-09-23

状态：候选协议代码已通过开发机与实体 Pi 的自动化测试，并以本机隔离 Broker 接入 F407，取得英文及中英混合文本的真实回显。2026-09-23 又将提交 `0b86a59` 部署到生产服务，经现网 Broker 直达 MQTT 的受限回显成功。跨端文档《2026-09-23-阶段一H1真实回显跨端契约》仍为候选稿；本端部署不等于来源门禁或 Server/Web 闭环完成。[隔离实机验收记录](2026-09-23-H1树莓派隔离实机验收记录.md)与[现网部署记录](2026-09-23-H1树莓派现网部署记录.md)保留真实消息和完整帧。

## 已实现的本端行为

- 已持久化绑定的主控箱沿用 `{namespace}/{device_id}/experiment/set`、QoS 2 订阅和重连恢复；`inspect_pi_link` 继续原有只读分支。新 `uart_echo` 严格校验设备目标、动作和会话 UUID、正数租约版本、UTC 毫秒有效期、精确字段以及编码后 1—48 字节的受限 UTF-8 文本。`HELLO CNS` 不是代码常量。无法安全关联动作的非法请求只记录中文原因，不猜测 ACK。
- H1 发帧要求本次串口会话的 F407 身份已与持久化绑定核验一致、身份帧由所学习的 F407 `sysid/compid` 端点发出、唯一业务串口已打开，且没有另一项下行控制事务。`cached` 身份仍可用于原只读自检和 MQTT 控制入口，但不能发送 H1。串口已打开不表示 UART6 收发成功。
- 同一进程的 `MavlinkLink` 在现有 fd 写入候选 `TUNNEL payload_type=0x8003` 请求，并在原有接收路径保留 CRC 通过的完整 RX 线上字节；没有第二个串口读取者，也不改波特率。Pi 波特率从该 fd 的 termios 已应用值读取，F407 波特率只取匹配回显中的报告值。匹配时同时核对 F407 来源 sysid、compid、Pi 目标、版本、长度、随机 nonce 和原样文本。有效的新业务帧必须在本动作发送后由现有解码器实际识别；心跳、身份元数据与旧遥测缓存不算。成功事实含 TX/RX 完整帧十六进制与字节数、两端报告波特率、原样请求/响应文本、nonce 和同一 Pi 单调时钟的微秒 RTT。
- 发帧前将 `action_id` 与完整动作比较内容写入 `identity.binding_file` 同目录的 `<绑定文件名>.h1-actions.json` 并同步到磁盘；可关联动作的预检拒绝也保存终态。最多保留 128 个持久动作标记，不按有效期自动删旧 ID；达到上限时拒绝新增 H1，防止回收旧 ID 后再次物理执行。终态落盘在改名之前失败时返回 `result_uncertain` 并禁用后续 H1；若改名已完成但目录同步失败，则结果可能已经可见，同进程后续请求也暂不应答，留待重新装载记录，避免前后发布相矛盾的结果。重启后的未决记录只返回 `result_uncertain`，不盲目再次发送。同动作同内容的已确定终态可复用，改内容返回 `duplicate_conflict`。该文件是运行时状态，不是示例配置，也不得放入 Git。长期使用超过 128 项前，须由三端另行冻结安全的扩容/归档策略，不能直接清空去重记录。
- 终态沿用 `{namespace}/{device_id}/experiment/ack`、QoS 2、非 retained 和原有内存 ACK 待发队列；MQTT 发布调用仅表示已入队，必须等 QoS 完成回调才移出待发队列。超时、断线或重连后只重试同一 ACK，不重发 UART 请求。重启恢复的历史 ACK 每次仅在队列空闲时补入一条，不占满队列妨碍实时只读自检。回显等待及 ACK 确认均由主循环非阻塞推进；期间注册、遥测及原只读自检继续处理。配置和飞控命令与 H1 下行互斥。串口失联、身份不再核验、错误回显、超时或缺失新业务帧均不生成成功事实。

## 尚未冻结的边界

1. 2026-09-23 核对到生产 Pi 提交为 `a618548`、配置路径为 `/var/lib/cns-rpi/config.json`，其中 `serial.device=auto`、`serial.baud=115200`。CH340（`1a86:7523`）枚举为 `/dev/ttyUSB0`；隔离实测时唯一 H1 进程接管该端点，Pi 从已打开 fd 取得实际应用速率 115200，F407 在匹配回显中也报告 115200。当前双方通信速率 B0 已有实体帧佐证，但 USB 桥到 F407 UART6 的逐线映射和 F407 实烧版本仍**未核验**。该桥曾短暂出现 USB `-71` 错误，恢复后再次识别设备。
2. 当前实体 F407 已对 `TUNNEL payload_type=0x8003` 的英文与中英混合文本返回匹配 nonce、原样文本和完整 RX 帧，证明本次接入的固件具备所测 H1 回显能力；不能据此推断固件精确版本或所有负向路径均通过。`5 秒`回显超时和`10 秒`新业务帧截止值仍是本端候选值，不构成三端冻结值；后续冻结结果不同须同步契约、代码和测试。
3. Pi 校验 `session_id` 和 `lease_version` 的字段格式与有效期，不持有 Server 的活动租约权威状态；Session 归属、撤销和来源 MQTT ACL 仍须由 Server/Broker 侧处理。2026-09-23 只读核对云端 Mosquitto 实际监听 `0.0.0.0:1883` 且 `allow_anonymous true`，未发现密码文件或 Topic ACL；从开发机匿名通配订阅也实际发现了设备 Topic。用户本轮决定先部署受限回显、MQTT 权限后续处理；这不证明来源门禁已满足。生产路由二进制及后端构建产物尚未检出 `uart_echo`，因此 Server/Web 全链路仍未验收。
4. ACK 在同一进程内未获得 QoS 完成确认会重试；进程重启后从持久化记录恢复已确定终态并再次发布，Server 须按动作身份处理重复 ACK。终态落盘在改名之前失败时给出 `result_uncertain`，此 ACK 不能保证在异常重启后自动恢复；改名后目录同步失败时暂不发布 ACK，Server 需按超时收敛。实体 MQTT 重连和 Server 入库未验证。
5. 本轮没有新增 `config.json` 字段，因此示例配置不变；也没有修改 F407、Server/Web、波特率或其他实验阶段。

## 自动化验证与隔离实机证据

在开发机隔离工作树运行：

```sh
cmake -S . -B /tmp/cns-rpi-h1-build -DCMAKE_BUILD_TYPE=Debug
cmake --build /tmp/cns-rpi-h1-build -j2
ctest --test-dir /tmp/cns-rpi-h1-build --output-on-failure
```

开发机完整构建和 45/45 项 CTest 通过，涵盖输入校验、候选 TUNNEL、伪终端完整帧、事务去重与超时，以及原有配置、飞控、注册、遥测和只读自检。另用开发机临时本地 Broker 设置 `CNS_TEST_MQTT_BROKER_PORT=18889`，验证 QoS 2 ACK 的完成回调可被主线程消费且不会重复消费；5 项 MQTT 测试、299 条断言均通过。2026-09-23 又将提交 `f0eefb2` 放入树莓派 `/tmp/cns-rpi-h1-20260923`，在 ARM64 上完成 Debug 构建和 45/45 项 CTest。随后仅在短时隔离测试中运行 H1，原生产配置未改；现场结果见[隔离实机验收记录](2026-09-23-H1树莓派隔离实机验收记录.md)。本次不等于 5G/MQTT 现网重连或 Server/Web 验收。

以下成功 ACK 用候选文档的 `HELLO CNS`、57600 和构造帧演示字段**形态**，并非本次程序在实体设备上采集的消息；`round_trip_us` 亦是示例值：

```json
{"schema_version":1,"command_id":"11111111-1111-4111-8111-111111111111","session_id":"33333333-3333-4333-8333-333333333333","action_id":"22222222-2222-4222-8222-222222222222","operation":"uart_echo","status":"completed","observed_at":"2026-09-23T10:00:00.000Z","fact":{"nonce":"0102030405060708","request_text":"HELLO CNS","response_text":"HELLO CNS","pi_baud_rate":57600,"f407_baud_rate":57600,"fresh_business_frame":true,"tx_frame_hex":"FD190000002ABF81010003802AC114010108070605040302010948454C4C4F20434E53FC90","tx_frame_bytes":37,"rx_frame_hex":"FD1B0000012AC181010003802ABF18010208070605040302010948454C4C4F20434E5300E1E19F","rx_frame_bytes":39,"round_trip_us":3000}}
```

拒绝 ACK 不带 `fact`，例如当前身份仅为缓存时：

```json
{"schema_version":1,"command_id":"11111111-1111-4111-8111-111111111111","session_id":"33333333-3333-4333-8333-333333333333","action_id":"22222222-2222-4222-8222-222222222222","operation":"uart_echo","status":"rejected","observed_at":"2026-09-23T10:00:00.000Z","error_code":"identity_unavailable"}
```

## 后续现网全链路验收

1. 由 F407 负责人补充实烧版本与 UART6 逐线接线记录，核对本次已测的双方 115200 速率事实。三端结合本次真实完整帧冻结 `0x8003` 载荷、字节序、目标端点、长度与零尾裁剪、超时、ACK 状态与错误码，以及纯中文和负向字节向量。
2. 现网直达 MQTT 的一次真实回显已取得完整帧；后续仍需核对并加固来源 ACL 与活动 Session 门禁，再验证由 Server 正式派发的请求。负向现场向量覆盖错 nonce、错来源组件、错文本、错波特率、静默、拔线与重启重投。
3. Server 逐项核对并落库 ACK、REST/WebSocket/Web 展示，与 Pi 原始 MQTT 结果相符；旧 `inspect_pi_link`、配置、飞控、注册和遥测仍正常。没有 Server 数据库记录时，不宣称跨端 H1 完成。
