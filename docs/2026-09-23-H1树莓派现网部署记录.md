# 2026-09-23 H1 树莓派现网部署记录

## 范围与提交

本轮按用户指示先合并并部署树莓派端的受限文本回显，匿名 MQTT 权限留待后续处理。不修改 F407、Server/Web 或现场配置，不把 Pi↔F407 单项验证写成跨端 H1 完成。

- `codex/h1-uart-echo-design` 快进合并到 `main`；本次部署的代码提交为 `0b86a59355db505ac460207b67f023277a75e1b5`，已推送远端 `main`。程序输出版本号仍是 `1.0.2`，本轮未创建新标签或 Release。
- 开发机在合并前、合并后分别完成 Debug 构建和 45/45 项 CTest；实体 Pi 上执行 `./scripts/deploy.sh`，Release 构建、现场配置校验和两个 systemd 服务安装/重启均完成，随后 45/45 项 CTest 通过。
- 物联卡无法访问 GitHub 镜像，故通过经验证的 Git bundle 将同一 `main` 提交传入 Pi，再以快进方式更新；没有修改 Pi 的 Git 远端或网络配置。Pi 部署后工作区干净。

## 现场服务与配置

部署前已在 Pi 的 `/var/lib/cns-rpi/backups/h1-20260923/` 保存旧二进制、现场配置和两个原 systemd 单元文件；目录权限为 0700，配置副本权限为 0600。部署前后 `/var/lib/cns-rpi/config.json` 的 SHA-256 均为 `6661f165d116aaed9d508225ff1462e663ff221854b2b44c7c419754534ef925`，说明本次没有改动现场配置。

部署后 `cns-rpi.service` 与 `cellular-dialup.service` 均为 `active`、`enabled`，检查时无自动重启记录。主程序 PID 7380 独占 `/dev/ttyUSB0`；日志依次出现 MQTT 已连接、F407 身份 `DCDWCNS1GHC0G6LF8MY6` 已核验、业务在线。以上 PID 与状态是 2026-09-23 的现场快照，不作为永久不变值。

## 现网直达 MQTT 回显

在实体 Pi 上向现网 Broker `112.124.52.232:1883` 的 `cns_rpi/DCDWCNS1GHC0G6LF8MY6/experiment/set` 发布一条受限文本请求，订阅对应 `experiment/ack`，均使用 QoS 2。请求直接由测试客户端发布，没有经过 Server 的活动 Session 路由：

```json
{"schema_version":1,"command_id":"85ecc8bd-ffe9-42d5-aa58-dd9d6c5b6854","request_id":"a5da0237-d618-4837-ab87-e21592942a96","target":{"device_id":"DCDWCNS1GHC0G6LF8MY6"},"session_id":"b9ce4e3c-a046-4a27-bdd1-79509e230fe6","action_id":"a5da0237-d618-4837-ab87-e21592942a96","lease_version":1,"operation":"uart_echo","text":"H1 MAIN 01","expires_at":"2026-09-23T11:38:39.993Z"}
```

Pi 进程收到 F407 的真实回显后发布以下原始 ACK；请求现已过期，不能直接重放：

```json
{"action_id":"a5da0237-d618-4837-ab87-e21592942a96","command_id":"85ecc8bd-ffe9-42d5-aa58-dd9d6c5b6854","fact":{"f407_baud_rate":115200,"fresh_business_frame":true,"nonce":"d76f8e04c1dc2ca0","pi_baud_rate":115200,"request_text":"H1 MAIN 01","response_text":"H1 MAIN 01","round_trip_us":10954,"rx_frame_bytes":41,"rx_frame_hex":"FD1D0000094CC181010003804CBF190102A02CDCC1048E6FD70A4831204D41494E20303100C201CD15","tx_frame_bytes":38,"tx_frame_hex":"FD1A0000004CBF81010003804CC1150101A02CDCC1048E6FD70A4831204D41494E203031DD10"},"observed_at":"2026-09-23T11:37:11.254Z","operation":"uart_echo","schema_version":1,"session_id":"b9ce4e3c-a046-4a27-bdd1-79509e230fe6","status":"completed"}
```

请求与回显文本逐字节相同；TX/RX 十六进制独立复算分别为 38/41 字节，与 ACK 相符；双方报告速率均为 115200，动作后新业务帧为真，单调时钟 RTT 为 10954 微秒。Pi 日志记录了同一 `action_id` 的串口发帧。该结果验证了生产 Pi 经现网 Broker 到 F407 的所测回显路径，但没有验证 Server 入库、REST/WebSocket/Web 显示或由 Server 创建的真实 Session。

## 后续边界

H1 只允许 1—48 字节受限 UTF-8 文本原样回显，不执行 SQL 或任意串口命令。本次从开发机只读匿名订阅 `cns_rpi/+/runtime/status/v1`，实际收到 `cns_rpi/DCDWCNS1GHC0G6LF8MY6/runtime/status/v1`；因此只要客户端可达 Broker，设备 Topic 可以通过通配订阅发现，不依赖 Web 或登录 Pi。Pi 目前只校验 `session_id`、`lease_version` 等请求字段，不掌握 Server 的活动 Session 权威状态。用户决定本轮不以匿名 MQTT 阻塞部署；来源控制与活动 Session 门禁仍是后续事项，不因本次回显成功而视为已完成。

若需回退，旧二进制与原配置保存在上述备份目录，旧代码提交为 `a618548344387ec6ad59c59acf20c8371b911fdb`。回退前应先停止主服务并核对代码、二进制及配置版本，不直接删除 H1 去重记录或覆盖当前现场配置；本轮未执行回退。
